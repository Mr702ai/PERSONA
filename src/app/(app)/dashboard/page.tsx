export default function DashboardPage() {
    return (
        <main className="page-shell">
            <h1>Dashboard</h1>
            <p>Overview of active sessions and metrics.</p>
        </main>
    );
}
